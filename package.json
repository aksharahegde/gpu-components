{
  "name": "gpu-component-runtime",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present && npm run typecheck:registry",
    "typecheck:registry": "tsc -p registry/tsconfig.json --pretty",
    "test": "npm run test --workspaces --if-present && npm run test:registry",
    "test:registry": "node --experimental-strip-types --import ./registry/timeline/test/register.mjs --test registry/timeline/*.test.ts registry/timeline/*.test.tsx registry/heatmap/*.test.ts registry/heatmap/*.test.tsx registry/grid/*.test.ts",
    "//test:registry": "the glob above already picks up render.pixels.test.ts, which renders through real Dawn (vgpu/node) and skips itself if Dawn is unavailable"
  }
}
