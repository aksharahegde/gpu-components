{
  "name": "@gpu-components/core",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Framework-independent GPU runtime for gpu-components, built on vgpu.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit --pretty",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "dependencies": {
    "vgpu": "^0.3.1"
  },
  "devDependencies": {
    "@gpu-components/testing": "*",
    "@types/node": "^22.10.2",
    "@webgpu/types": "^0.1.72",
    "typescript": "^5.6.3"
  }
}
